apiVersion: v2
name: inference
description: Gemma LLM Inference Service, Model Loader, and Alerting for Lang-Learn
type: application
version: 0.2.0
appVersion: "1.1.0"

dependencies:
  - name: minio
    version: "5.4.0"
    repository: "https://charts.min.io/"
    condition: minio.enabled
